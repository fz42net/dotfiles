[user]
	name = Phil Thomas
	email = phil@fz42.net
[filter "lfs"]
	smudge = git-lfs smudge -- %f
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f

[init]
	defaultBranch = main

[core]
    hooksPath = ~/.config/githooks
	editor = vim
